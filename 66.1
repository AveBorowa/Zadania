#include <iostream>
#include <fstream>

using namespace std;

ifstream liczby("trojki.txt");

int main() {
int a,b,c;
int _a,_b,_c;
int s1,s2;

while (liczby >> a >> b >> c) {
 _a = a,
 _b = b,
 _c = c;

 s1=0,s2=0;

 while (a >= 10 || b >= 10 || c >= 10) {
  if (a>=10) {
   s1 += a%10;
   a = a/10;
   
  } else if ( b >= 10) {
   s1 += b%10;
   b = b/10;

  } else if (c >= 10) {
   s2 += c%10;
   c = c/10;
   
};};

 s1 += a += b;
 s2 += c;

 if (s1 == s2) {
  cout << _a << " " << _b << " " << _c << endl;
 }
};
return 0;
}

